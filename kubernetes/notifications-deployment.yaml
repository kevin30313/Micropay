apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-deployment
  namespace: micropay
  labels:
    app: notifications-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
        version: v1
    spec:
      containers:
      - name: notifications-service
        image: micropay/notifications:latest
        ports:
        - containerPort: 3004
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: micropay-config
              key: NODE_ENV
        - name: NOTIFICATIONS_PORT
          valueFrom:
            configMapKeyRef:
              name: micropay-config
              key: NOTIFICATIONS_PORT
        - name: JWT_SECRET
          valueFrom:
            configMapKeyRef:
              name: micropay-config
              key: JWT_SECRET
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: micropay
  labels:
    app: notifications-service
spec:
  selector:
    app: notifications-service
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
  type: ClusterIP